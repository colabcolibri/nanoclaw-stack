/**
 * UI re-export — single source of truth lives in nanoclaw motor.
 */
export {
  pickDefaultSessionId,
  resolveSelectedSessionId,
  type SelectableThread,
  type ThreadStatus,
} from '../../nanoclaw/src/conversations/thread-selection.js';
